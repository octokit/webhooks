{
  "name": "@octokit/webhooks-definitions",
  "version": "0.0.0-development",
  "publishConfig": {
    "access": "public"
  },
  "description": "machine-readable, always up-to-date GitHub Webhooks specifications",
  "main": "index.json",
  "devDependencies": {
    "@octokit/request": "^5.0.0",
    "ajv": "^7.0.0",
    "cheerio": "^1.0.0-rc.2",
    "execa": "^5.0.0",
    "fs-extra": "^9.0.0",
    "json-diff": "^0.5.3",
    "semantic-release": "^17.0.0",
    "turndown": "^7.0.0",
    "yargs": "^16.0.0"
  },
  "scripts": {
    "build": "node bin/octokit-webhooks.js check --cached",
    "pretest": "npm run -s lint",
    "test": "npm run build && npm run validate && node test.js",
    "lint": "prettier --check '{bin,lib}/*.js' '*.{js,md}' 'payload-examples/**/*.json' *.md package.json index.json",
    "lint:fix": "prettier --write '{bin,lib}/*.js' '*.{js,md}' 'payload-examples/**/*.json' 'payload-schemas/**/*.json' *.md package.json index.json",
    "validate": "node bin/octokit-payloads.js"
  },
  "repository": "https://github.com/octokit/webhooks",
  "keywords": [],
  "author": "Gregor Martynus (https://github.com/gr2m)",
  "license": "MIT",
  "files": [
    "index.d.ts",
    "index.json"
  ],
  "dependencies": {
    "prettier": "^2.0.5"
  }
}
